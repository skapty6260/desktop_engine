Отдельная папка с xml файлами и meson.build, которые будут генерироваться в СИ файлы либой.

Система должна быть модульной где я буду прописывать xml интерфейсы и просто через типизацию и биндинг к структурам и методам сервера они будут сами хандлится

Нужно интегрировать в wayland event loop через addfd.

Получаются файлы:
Интеграционный файл (Интегрирует систему с wayland event loop)
Инициализационный файл (Инициализирует систему, содержит главные структуры)
Модульный файл (Нужен для обработки и структур для создания модулей a.k.a любых методов и запросов по xml интерфейсам)

